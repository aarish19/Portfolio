import React from "react";

const About = () => (
  <section style={styles.section}>
    <h2>About Me</h2>
    <p>
      I'm a passionate web developer skilled in React and Javascript.
      I love turning ideas into reality through code.
    </p>
  </section>
);

const styles = {
  section: { padding: "2rem", maxWidth: "600px", margin: "auto" },
};

export default About;
